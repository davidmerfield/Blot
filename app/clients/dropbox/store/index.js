// var DROPBOX = {cursor: 'string', id: 'string', token: 'string'};

// dropbox:      [DROPBOX,   true,   false],

//       if (changes.dropbox && latest.dropbox && latest.dropbox.id) {
//         client.sadd(key.dropbox(latest.dropbox.id), blogID);
//       }

//   dropbox: function(account_id) {
//     return 'dropbox:' + account_id;
//   },
