location = /purge {
    if ($request_method = PURGE) {
        # if you edit this path also update proxy_cache_path in http.conf
        set $lua_cache_directory "/var/www/blot/data/openresty_cache";
        content_by_lua_file /var/www/blot/config/nginx/purge.lua;

    }
}