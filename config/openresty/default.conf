http {
    events {
        worker_connections 10000;
        multi_accept on;
    }

    server {
        listen       80;
        server_name  localhost;

        location / {
            return 200 'hello world';
        }

    }
}